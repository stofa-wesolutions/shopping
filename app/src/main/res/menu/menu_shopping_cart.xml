<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".ShoppingCart">

    <!-- add article to cart button -->
    <item
        android:id="@+id/action_add_to_cart"
        android:icon="@drawable/icon_add_to_cart"
        android:title="@string/action_add_to_cart"
        app:showAsAction="ifRoom"
    />

    <!-- refresh cart button -->
    <item
        android:id="@+id/action_refresh_cart"
        android:icon="@drawable/icon_refresh_cart"
        android:title="@string/action_refresh_cart"
        app:showAsAction="ifRoom"
    />

    <!-- settings -->
    <item
        android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        app:showAsAction="never"
    />
</menu>
